import styled from 'styled-components';
import { GRAY_DARK } from 'Styled/Settings/colors';

export const StyledSettingPage = styled.div`
  .setting-page__general-setting {
    background-color: ${GRAY_DARK};
    padding: 0 15px;
    line-height: 48px;
    font-size: 18px;
  }
`;
