import React from 'react';
import { PAGE_NAME } from 'Styled/Settings/constants';
import {
  HeaderContainer,
} from '../Styled';

export default () => (
  <HeaderContainer>
    {PAGE_NAME.TRAVEL_WALL}
  </HeaderContainer>
);
